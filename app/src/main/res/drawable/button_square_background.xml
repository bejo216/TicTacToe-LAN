<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>
    <item android:state_pressed="true" android:drawable="@drawable/button_pressed"/><!-- Button background color -->
    <corners android:radius="30dp"/> <!-- Set radius to 0 to remove round corners -->
    <stroke android:color="#000000" android:width="-10dp"/> <!-- Optional: add a border -->
</shape>